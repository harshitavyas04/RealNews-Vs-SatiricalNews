from setuptools import setup, find_packages

setup(
    name="RealNews-Vs-SatiricalNews",
    version="0.1",
    packages=find_packages(),
    install_requires=[],  # List your dependencies here or in requirements.txt
)
